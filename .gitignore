*.bun-build

# 作業中の控え（検査が未コミットとして検知するため）
*.bak-*

# ビルド成果物。配布に不要（.mcp.json は server.ts を指す）
# ★依存ライブラリの __dirname に、ビルドした機械のパスが焼き込まれる
server.js
/nhack-premium
/scripts/danger-check.mjs
/scripts/prepare-cache.mjs

# ★配布物から外す（2026-09-07）
#   お客様の機械には .git ごと届く（実測: 80コミット）。
#   うちの開発道具・開発運用は、届いた先で読まれる意味がない。
#   ローカルには残す（消すと build-hook.sh も PR の型も使えなくなる）。
.hook-test/
hooks-claude/_src/
.github/pull_request_template.md
